// Intentionally left blank.
//
// The local CocoaPods project may retain a reference to this shared source file
// across incremental builds. Keeping the file present avoids stale build input
// errors without duplicating the button-specific gradient implementation.
